module.exports = {
	// TODO: Determine if this preset is needed, and add if so.
	// preset: '@wordpress/jest-preset-default',
	testPathIgnorePatterns: [
		'/wp-content/plugins/',
		'/wp-content/themes/emilygarfield',
		'/node_modules/',
	],
};

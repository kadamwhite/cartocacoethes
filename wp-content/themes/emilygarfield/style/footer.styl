@import './_mixins';

/* =Footer
----------------------------------------------- */

#colophon {
  clear: both;
}
#supplementary {
  border-top: 1px solid #ddd;
  padding-top: 1em;
  +respond-above($sm-screen) { padding: 2em 1em 0; }
  +respond-above($md-screen) { padding: 2em 2em 0; }
  overflow: hidden;
}

footer .widget-area {
  float: left;
  margin-right: 3.7%;
  width: 30.85%;

  ul {
    margin-left: 0;
  }

  li {
    display: inline-block;
    &:not(:last-child) {
      a::after {
        content: ',';
      }
    }
  }

  +respond-below($sm-screen) {
    float: none;
    margin: 0 1em;
    width: auto !important;
    text-align: center;
  }
}

footer .widget-area + .widget-area + .widget-area {
  margin-right: 0;
}

footer .widget-area-full-width {
  float: none;
  clear: both;
  width: 100%;
  margin: 3.7% 0 0;
  padding-top: 3.7%;
  text-align: center;
  border-top: 1px solid #ddd;
  +respond-below($sm-screen) {
    margin: 1em 0 0;
  }

  .widget-title {
    text-align: center;
  }
}

/* Site Generator Line */
#site-generator {
  background: #f9f9f9;
  border-top: 1px solid #ddd;
  color: #666;
  font-size: 12px;
  line-height: 2.2em;
  padding: 2.2em 0.5em;
  text-align: center;
}
#site-generator p:last-of-type {
  margin-bottom: 0;
}
#site-generator a {
  color: #555;
  font-weight: bold;
}

// Hide the "Proudly Powered by WordPress"
#site-generator > a:last-child {
  display: none;
}

.copyright-more-info {
  -moz-box-sizing: border-box;
  box-sizing: border-box;
  margin: auto;
  max-width: 600px;
  padding: 1.625em 1em 0;
  text-align: center;
}
// Overrule the assistive-text styles when the anchor is present in the URL,
// to provide a low-impact no-JS fallback
.copyright-more-info:target {
  height: auto;
  overflow: auto;
  position: static !important; // overrides .assistive-text
  width: auto;
  clip: auto;
}
